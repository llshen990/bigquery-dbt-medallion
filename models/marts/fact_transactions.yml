version: 2

models:
  - name: fact_transactions
    description: "Fact table containing all e-commerce transactions"
    columns:
      - name: order_item_id
        tests:
          - unique
          - not_null
      - name: customer_id
        tests:
          - not_null